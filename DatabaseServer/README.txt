===========================
Install node
-------
(In this directory)

Setup config.json (required) to run with the currently running MySQL instance
 
Run 
    `npm i`
    `npm i -g forever`

Run `npx forever start ./api.js` 
